application.title=UasPbo
application.vendor=kelompok
build.dir=build
build.classes.dir=${build.dir}/classes
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
dist.dir=dist
dist.jar=${dist.dir}/UasPbo.jar
dist.javadoc.dir=${dist.dir}/javadoc
endorsed.classpath=
excludes=
file.reference.mysql-connector-j=lib/mysql-connector-j.jar
includes=**
jar.compress=false
javac.classpath=\
    ${file.reference.mysql-connector-j}
javac.compilerargs=-Xlint:unchecked
javac.deprecation=false
javac.source=1.8
javac.target=1.8
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
javadoc.additionalparam=
javadoc.encoding=${source.encoding}
jaxbwizz.endorsed.dir=${build.dir}/jaxbwizz.endorsed.dir
jaxbwizz.endorsed.classpath=\
    ${jaxbwizz.endorsed.dir}/
jnlp.codebase=file:/${basedir}/dist/
jnlp.descriptor=application
jnlp.enabled=false
jnlp.mixed.code=true
jnlp.mixed.code.trusted=true
jnlp.offline.allowed=true
jnlp.resource=
jnlp.signed=false
jnlp.signing.alias=
jnlp.signing.keystore=
jnlp.signing.storepass=
main.class=com.uaspbo.Main
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
meta.inf.disabled=
meta.inf.signed=
platform.active=default_platform
run.jvmargs=
run.jvmargs.ide=
src.dir=src
test.src.dir=test
source.encoding=UTF-8
